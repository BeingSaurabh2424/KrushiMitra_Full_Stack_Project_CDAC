server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/krushimitra_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

jwt:
  secret: krushimitrasecretkeyforsigningjwttokens2024
  expiration: 86400000 # 24 hours in milliseconds

logging:
  level:
    com.krushimitra: DEBUG
    org.springframework.security: DEBUG