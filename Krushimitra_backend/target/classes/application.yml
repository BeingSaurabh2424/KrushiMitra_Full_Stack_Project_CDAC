server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/krushimitra_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true



logging:
  level:
    com.krushimitra: DEBUG
    org.springframework.security: DEBUG